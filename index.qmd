---
title: "ビジュアライゼーション"
subtitle: "データの構造を色にする：3種類のカラーパレットを使いこなそう"
---

## {ggplot2}

- R でデータやモデルの可視化を行うときのモダンな選択肢は {ggplot2}.

```{r}
library(ggplot2)
library(midr)
library(midnight)
library(ggthemes)
```


```{r gapminder}
#| fig-height: 5
#| fig-width: 7.5
library(gapminder)
p1 <- ggplot(
  data = gapminder,
  mapping = aes(
    x = gdpPercap,
    y = lifeExp,
    color = continent
  )
) +
  geom_point() +
  geom_smooth(method = "gam", formula = y ~ s(x)) +
  scale_x_log10() +
  scale_color_theme(theme = "moon") +
  scale_fill_theme(theme = "moon")
p1 +
  theme_midr(grid = "xy") +
  theme(legend.position = "bottom") +
  labs(x = "GDP per Capita", y = "Life Expectancy")
```

```{r life_expectancy}
#| fig-height: 5
#| fig-width: 7.5
library(rnaturalearth)
library(dplyr)
library(fmsb)
japan <- ne_states(
  country = "japan",
  returnclass = "sf"
) |>
  mutate (
    name = name |>
      gsub(pattern = "Ō", replacement = "O") |>
      gsub(pattern = "ō", replacement = "o") |>
      gsub(pattern = "Kochi", replacement = "Kouchi")
  ) |>
  left_join(
    Prefe0 |> select(PNAME, E0F.2020),
    by = c("name" = "PNAME")
  )

legend_label = "Life Expectancy at Birth\n(Female, 2020)"

ggplot(data = japan) +
  geom_sf(
    mapping = aes(
      color = E0F.2020,
      fill = E0F.2020
    )
  ) +
  scale_color_theme("eclipse", middle = 87.74) +
  scale_fill_theme("eclipse", middle = 87.74) +
  coord_sf(
    xlim = c(128, 146), ylim = c(30, 45)
  ) +
  labs(fill = legend_label, color = legend_label) +
  theme_midr(grid = "xy") +
  theme(legend.key.height = unit(40, "pt"))
```

```{r}
#| fig-height: 5
#| fig-width: 7.5
library(ggrepel)
ggplot(mtcars, aes(x = hp, y = mpg)) +
  geom_smooth(method = "gam", formula = y ~ s(x, bs = "cs"), 
              color = "grey80", fill = "grey90", alpha = 0.5) +
  geom_point(aes(size = wt, color = factor(cyl)), alpha = 0.8) +
  geom_text_repel(
    aes(label = rownames(mtcars)),
    size = 3,
    box.padding = 0.4,
    point.padding = 0.2,
    segment.alpha = 0.5,
    max.overlaps = 15
  ) +
  scale_size_continuous(range = c(1, 8)) +
  scale_color_brewer(palette = "Set1") +
  labs(
    x = "Gross Horsepower",
    y = "Fuel Efficiency (Miles per Gallon)",
    size = "Weight",
    color = "Cylinders"
  ) +
  theme_midr(grid = "xy")
```


Wickham, Hadley A. (2008). "Practical tools for exploring data and models" [link](https://dr.lib.iastate.edu/entities/publication/9210139e-102e-4537-8b6e-7d8197e2f3d8)
















### 散布図：点の位置でデータを表現する

```{r}
#| fig-height: 5
#| fig-width: 5
ggplot(data = diamonds) +
  geom_point(mapping = aes(x = carat, y = price))
```

```{r}
#| fig-height: 5
#| fig-width: 5
plot(price ~ carat, data = diamonds)
```

### 棒グラフ：棒の長さでデータを表現する

-   質的変数の水準別観測数

```{r}
#| fig-height: 4
#| fig-width: 7
ggplot(data = diamonds) +
  geom_bar(mapping = aes(x = clarity))
```

```{r}
#| fig-height: 4
#| fig-width: 7
diamonds$clarity |> table() |> barplot()
```

```{r}
#| fig-height: 4
#| fig-width: 7
ggplot(diamonds |> dplyr::count(clarity)) +
  geom_col(aes(x = clarity, y = n))
```

### ヒストグラム：棒の長さで連続変数の分布を表現する

ヒストグラムは「棒の間の隙間を空けない」のが通例

棒グラフ: 項目が独立しているので、隙間を空ける。

ヒストグラム: 数値が連続していることを示すために、棒をくっつける。

```{r}
#| fig-height: 5
#| fig-width: 5
ggplot(diamonds) +
  geom_histogram(aes(x = carat), bins = 20)

```

折れ線グラフ：データの推移を線で表現する

```{r}


```

### 箱ひげ図

-   ggplot2 extensions https://exts.ggplot2.tidyverse.org/

-   all your figure are belong to us https://yutannihilation.github.io/allYourFigureAreBelongToUs/
